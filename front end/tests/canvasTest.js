//rect parameters = (x, y, width, height) 
const canvas = document.getElementById('canvas');
const context = canvas.getContext("2d");
context.fillStyle = "#FFFFFF";
context.fillRect(325, 475, 100, 25);